# پرامپت اصلاحیه — این را به همان AI بده

> طرز استفاده: هر سه فایل را دوباره به AI بده، بعد این بلوک را کامل کپی کن و بفرست.
> اگر محدودیت طول داری، آن را به دو بخش بشکن: «بخش A» (فایل‌های ۰۱ و ۰۳) و «بخش B» (فایل ۰۲).

---

```
من سه فایلی که برایم ساختی را یک بازبینی فنی کردم. استخوان‌بندی سیستم خوب است و
نمی‌خواهم از صفر بازنویسی شود. ۱۶ مشکل مشخص پیدا شده که باید نقطه‌ای اصلاح شوند.

قوانین کلی این اصلاحیه:
- چیزی را که در لیست نیست تغییر نده.
- NICHE LOCK، منطق ORIGINAL LAYER، روش CHARACTER SHEET، و قانون «نریشن یک‌جا با TTS»
  دست‌نخورده بمانند. این‌ها تصمیم‌های درستی بودند.
- هر تغییری در قوانین قفل‌شده باید همزمان در هر سه فایل اعمال شود.
- برای هر مورد بنویس دقیقاً در کدام فایل و کدام بخش تغییر دادی.

═══════════════════════════════════════════════════
بخش A — تناقض بین فایل‌ها (اول این‌ها)
═══════════════════════════════════════════════════

۱) PRISM TRANSITION در هر سه فایل سه جور نوشته شده — حتی center/centre هم فرق دارد.
   یک امضا نمی‌تواند سه توصیف داشته باشد. از این لحظه فقط این نسخه معتبر است و
   عیناً، کلمه‌به‌کلمه، در هر سه فایل جایگزین شود:

   PRISM TRANSITION: A single beam of warm amber light strikes an invisible prism at
   the exact centre of the frame and splits into a fan of cyan and magenta rays. The
   scene fractures along those rays into several parallel versions of itself, fanning
   outward. The subject stands at the exact centre in full silhouette, small, facing
   away from camera, black against the light.

۲) جدول زمان‌بندی فایل ۰۱ (هوک 0:00–0:04) با فایل‌های ۰۲ و ۰۳ (هوک 0:00–0:09) نمی‌خواند.
   بدتر: فایل ۰۱ در یک جا می‌گوید QUESTION در 0:04–0:10 است و چند خط پایین‌تر می‌گوید
   «PRISM همیشه در ثانیه‌ی ۸ تا ۱۲». این تناقض داخلی خود فایل ۰۱ است.

   این جدول واحد را در هر سه فایل بگذار:
     HOOK          0:00–0:10   لحظه‌ی انسانی، بدون هیچ فکتی
     QUESTION      0:10–0:13   جمله‌ی What if + PRISM TRANSITION
     EXPLORATION   0:13–0:30   واقعیت ماجرا با عدد
     TWIST         0:30–0:44   نقطه‌ی عطف ضدشهود
     CONSEQUENCE   0:44–0:51   معنایش چیست
     LOOP ENDING   0:51–0:57   بازگشت بصری به فریم اول

   قانون «PRISM در ثانیه‌ی ۸ تا ۱۲» را کامل حذف کن. جایش:
   «PRISM دقیقاً روی جمله‌ی What if می‌افتد، هر ثانیه‌ای که آن جمله باشد.»

۳) قرارداد نام‌گذاری فایل‌ها یکسان نیست: فایل ۰۱ می‌گوید charsheet_[video-id].png ولی
   فایل ۰۳ می‌گوید charsheet.png — دومی روی ویدیوی دوم فایل اولی را overwrite می‌کند.
   در هر سه فایل این قرارداد را قفل کن:
     charsheet_ep[NNN].png
     still_ep[NNN]_[NN].png
     clip_ep[NNN]_[NN].mp4
     vo_ep[NNN].mp3

═══════════════════════════════════════════════════
بخش B — خطای محاسباتی زمان‌بندی (این‌ها ریاضی‌اند، نه سلیقه)
═══════════════════════════════════════════════════

۴) در فایل ۰۲ میانگین کلی درست است (۱۴۳ کلمه ÷ ۵۵ ثانیه = ۲.۶ w/s) ولی توزیعش خراب است.
   محاسبه‌ی پلان‌به‌پلان:
     پلان ۰۱: ۱۱ کلمه در ۳ ثانیه = ۳.۶۷ w/s  ← و این هوکی است که باید «زمزمه» باشد
     پلان ۰۲:  ۵ کلمه در ۳ ثانیه = ۱.۶۷ w/s  ← ۱.۲ ثانیه سکوت مرده
     پلان ۰۶: ۲۲ کلمه در ۶ ثانیه = ۳.۶۷ w/s
     پلان ۰۷: ۱۱ کلمه در ۶ ثانیه = ۱.۸۳ w/s
     پلان ۱۱: ۱۶ کلمه در ۵ ثانیه = ۳.۲۰ w/s

   جدول پلان‌بندی فایل ۰۲ را با فرمول (کلمات ÷ 2.75 + 0.45 ثانیه تنفس) بازمحاسبه کن.
   پلان ۰۶ با ۲۲ کلمه بیش از حد طولانی است — به دو پلان بشکن (06a و 06b) با دو
   اندازه‌ی نمای متفاوت. تعداد کل می‌شود ۱۲ که هنوز داخل بازه‌ی مجاز است.
   خروجی هدف: ۵۸ ثانیه، هیچ پلانی خارج از بازه‌ی ۲.۳ تا ۳.۱ کلمه بر ثانیه.

   در فایل‌های ۰۱ و ۰۳ هم همین فرمول را به‌عنوان قانون اجباری مرحله‌ی پلان‌بندی اضافه کن.

۵) اسکریپت VO در فایل ۰۲ دارد: ۱۴۵ کلمه + ۱۴ مکث کوتاه (۰.۳s) + ۱۰ مکث بلند (۰.۷s).
   یعنی ۱۱.۲ ثانیه سکوت خالص. حساب کن:
     ۱۴۵ ÷ ۲.۶ = ۵۵.۸ ثانیه گفتار + ۱۱.۲ مکث = ۶۷ ثانیه
   هدف ۵۵ ثانیه است و QA خودت می‌گوید «۵۰ تا ۵۹». یعنی ۸ ثانیه از QA خودت رد شده‌ای.
   کل بودجه‌ی مکث برای رسیدن به ۵۷ ثانیه فقط حدود ۳ ثانیه است.

۶) علامت `|` برای مکث در هیچ TTS‌ای کار نمی‌کند — نه ElevenLabs، نه بقیه. و `...` هم
   مکث با طول مشخص نمی‌سازد، فقط لحن کشیده و مردد می‌دهد؛ پس محاسبه‌ی «۰.۳ ثانیه»
   روی آن از پایه اشتباه است.
   اسکریپت VO را با این قوانین بازنویسی کن:
     - نقطه و کاما ریتم طبیعی می‌سازند؛ بگذار کارشان را بکنند
     - `...` فقط برای لحن مردد، نه به‌عنوان واحد زمانی
     - مکث با طول مشخص فقط در ۵ مرز بخش‌ها
     - برای مدل‌های v2:  <break time="0.5s" />
     - برای Eleven v3 (که SSML ندارد): [long pause]
     - بودجه‌ی کل مکث: حداکثر ۳ ثانیه در کل ویدیو
   هر دو نسخه (v2 و v3) را کنار هم بده چون معلوم نیست کاربر کدام مدل را دارد.

═══════════════════════════════════════════════════
بخش C — ادعاهای واقعیت‌محور که غلط‌اند
═══════════════════════════════════════════════════

۷) فایل ۰۱ می‌گوید «یوتیوب از ۲۰۲۶ محتوای قالبی را از مانیتایز حذف می‌کند». این غلط است.
   واقعیت: ۱۵ جولای ۲۰۲۵، و اصلاً قانون جدیدی نبود — یوتیوب فقط نام سیاست قدیمی
   «repetitious content» را به «inauthentic content» تغییر داد. Rene Ritchie رسماً گفت
   «یک به‌روزرسانی جزئی» است و این نوع محتوا «سال‌هاست غیرقابل مانیتایز بوده».
   خود ORIGINAL LAYER را نگه دار — ایده‌ی درستی است — فقط توجیهش را با واقعیت جایگزین کن.

۸) هر سه فایل می‌گویند فعال‌کردن «altered or synthetic content» اجباری است. برای این
   کانال اجباری نیست. سیاست رسمی یوتیوب صراحتاً محتوای «آشکارا غیرواقعی، انیمیشن،
   دارای جلوه‌ی ویژه» را معاف کرده. این کانال flat-cartoon animation است. صدای TTS هم
   تا وقتی صدای یک آدم واقعی را کلون نکند نیازی به افشا ندارد.
   در هر سه فایل، «اجباری» را با این جایگزین کن:
   «برای این استایل انیمیشنی طبق سیاست فعلی یوتیوب لازم نیست. اگر خواستی محض احتیاط
   بزن — روی ریچ و مانیتایز اثر منفی ندارد.»

۹) در فایل ۰۲، عبارت «About a hundred people» بیش‌ازحد است. منابع می‌گویند «کمتر از ۱۰۰»،
   و خود McGaugh گفته فقط حدود ۶۰ نفر واقعاً تأیید شده‌اند. چون کل هویت کانال روی
   «هر عدد قابل ردیابی است» بنا شده، این ریسک اعتبار دارد.
   جایگزین کن با: "Fewer than a hundred people" — هم دقیق‌تر است هم دراماتیک‌تر.
   SOURCE MAP را هم به‌روز کن.

۱۰) عنوان "The Woman Who Can't Forget" عیناً عنوان کتاب Jill Price است (۲۰۰۸، Free Press) —
    همان «AJ» در پژوهش ۲۰۰۶. دو مشکل: برای ویدیوی اول یک کانال صفر با یک کتاب مشهور
    بر سر همان کلیدواژه رقابت می‌کنی؛ و ویدیو درباره‌ی یک آدم واقعیِ قابل‌شناسایی است
    ولی شخصیت را Maya نامیده‌ای.
    ۵ عنوان جایگزین بده، همه زیر ۵۰ کاراکتر، هیچ‌کدام تداخل با عنوان آن کتاب.

۱۱) TWIST درست است ولی ناقص. Patihis et al. 2013 واقعاً نشان داد گروه HSAM نمره‌ی
    Overall False Memory بالاتری داشت (p = .041) — ولی دو قید مهم دارد:
      - این تفاوت پس از تعدیل برای absorption و fantasy proneness معنادار نماند
      - پس از source test، تعداد خاطرات کاذب دو گروه قابل‌مقایسه شد
    جمله‌ی اسکریپت را دست نزن (در ۵۵ ثانیه جا ندارد) ولی این قید را به DESCRIPTION اضافه کن.
    و در فایل‌های ۰۱ و ۰۳ این را به‌عنوان قانون دائمی اضافه کن:
    «هر وقت TWIST بر پایه‌ی یک یافته‌ی آماری است، محدودیت‌های آن یافته باید در توضیحات
    ویدیو بیاید. زودتر از منتقد گفتنش، اعتبار می‌سازد.»

۱۲) در فایل ۰۲ تایم‌لاین AJ در هم ادغام شده. AJ در ژوئن ۲۰۰۰ به McGaugh ایمیل زد؛
    مقاله در ۲۰۰۶ در Neurocase منتشر شد (Parker, Cahill & McGaugh). این دو را جدا کن.

═══════════════════════════════════════════════════
بخش D — بهبود فنی
═══════════════════════════════════════════════════

۱۳) STYLE LOCK می‌گوید سوژه در «دو-سوم بالایی» باشد. این نصفِ ماجراست — بالای فریم هم
    پوشانده می‌شود. سیف‌زون واقعی Shorts در 1080×1920:
      بالا 180px (نوار عنوان) | پایین 390px (نام کانال و دکمه‌ها)
      راست 120px (لایک/کامنت/سابسکرایب) | چپ 60px
      ناحیه‌ی امن ≈ 900×1350 در مرکز
    ستون ۱۲۰ پیکسلی سمت راست در هیچ‌کدام از سه فایل ذکر نشده — یعنی دکمه‌ی سابسکرایب
    می‌تواند روی صورت شخصیت بنشیند.
    جمله‌ی آخر STYLE LOCK را در هر سه فایل با این عوض کن و اسمش را بگذار STYLE LOCK v3:

    Vertical 9:16 frame. Compose the subject inside the central safe band: keep the top
    10 percent, the bottom 20 percent and the right 12 percent of the frame free of
    faces, eyes and any critical detail.

    بقیه‌ی بلوک بدون تغییر. چک‌لیست QA را هم به‌روز کن (الان فقط پایین را چک می‌کند).

۱۴) بلوک STYLE LOCK در پرامپت Character Sheet فایل ۰۲، ۷۶ کاراکتر کوتاه‌تر از بقیه است —
    جمله‌ی 9:16 حذف شده. این حذف منطقاً درست است (شیت ۳×۲ نباید ۹:۱۶ باشد) ولی چون
    قانون می‌گوید «کلمه‌به‌کلمه یکسان»، باید صریح نوشته شود:
    «تنها استثنای مجاز STYLE LOCK: در پرامپت Character Sheet، جمله‌ی مربوط به فریم عمودی
    و سیف‌زون حذف می‌شود. هیچ استثنای دیگری مجاز نیست.»

۱۵) مرحله‌ی ۷ فایل ۰۳ (VIDEO GENERATION) روی تقریباً هیچ ایجنتی اجرا نمی‌شود، و چون
    گفته‌ای Do not ask for permission between steps، ایجنت یا کل پایپ‌لاین را متوقف
    می‌کند یا الکی ادعا می‌کند فایل ساخته. یک مسیر جایگزین اضافه کن:
    «اگر توانایی image-to-video نداری: پرامپت‌های ویدیو را به‌صورت متنی تحویل بده،
    در گزارش نهایی صریح بنویس کدام مراحل اجرا نشدند، و ادامه بده. هرگز فایلی را که
    نساخته‌ای گزارش نکن.»
    همین قانون را برای TTS هم بنویس.

۱۶) این‌ها در هیچ‌کدام از سه فایل نیستند. به مرحله‌ی انتشار فایل ۰۱ و ۰۳ اضافه کن:
      - مشخصات خروجی: 1080×1920، H.264، فریم‌ریت ثابت، بیت‌ریت ۱۵–۲۵ Mbps
      - نرمال‌سازی صدا: یک هدف LUFS مشخص برای همه‌ی ویدیوها (بدون این، بلندی صدای
        هر ویدیو فرق می‌کند — بی‌کیفیت‌ترین سیگنال ممکن برای یک برند)
      - استایل زیرنویس: فونت، اندازه، رنگ، حاشیه، جایگاه عمودی — قفل‌شده برای کل کانال
      - ۳ نسخه‌ی جایگزین هوک در هر ویدیو (هوک تنها متغیر واقعاً تعیین‌کننده در Shorts است)
      - قانون شکست: اگر ریتنشن ثانیه‌ی ۳ زیر یک آستانه‌ی عددی مشخص بود، دقیقاً چه کار کنیم

═══════════════════════════════════════════════════
خروجی موردانتظار
═══════════════════════════════════════════════════

۱. هر سه فایل را کامل و اصلاح‌شده بازنویسی کن:
     01-what-if-prism-MASTER-PROMPT-v4.md
     02-VIDEO-01-FULL-PACKAGE-v2.md
     03-ONE-SHOT-AGENT-PROMPT-v2.md

۲. یک فایل چهارم بساز به نام CANON.md که فقط شامل بلوک‌های قفل‌شده باشد:
   STYLE LOCK v3، PRISM TRANSITION، جدول زمان‌بندی، فرمول پلان‌بندی، قرارداد نام‌گذاری،
   سیف‌زون، و مشخصات خروجی. هر سه فایل دیگر باید به این ارجاع بدهند نه اینکه کپی کنند.
   دلیل: الان این قوانین سه بار کپی شده‌اند و همین باعث شده از هم فاصله بگیرند.

۳. در انتها یک جدول بده: شماره‌ی مورد | فایل | بخش | چه چیزی عوض شد.

۴. اگر با کدام موردی مخالفی، اجرایش کن ولی دلیل مخالفتت را جداگانه بنویس.
   بحث را قبول می‌کنم، سکوت را نه.
```

---

## اگر بعد از اصلاحیه هم خواستی جلوتر بروی

این‌ها خارج از حوزه‌ی این سه فایل‌اند و باید جداگانه پرسیده شوند:

**۱. هویت کانال — هنوز هیچ‌جا نیست**
```
برای کانال What If Prism هویت کامل بساز: ۵ گزینه‌ی نام و هندل (با فرض اینکه
@whatifprism ممکن است گرفته شده باشد)، کانسپت آواتار و بنر با پرامپت تصویر
بر اساس STYLE LOCK v3، متن About زیر ۱۰۰۰ کاراکتر، ساختار پلی‌لیست‌ها، و
تیزر کانال ۳۰ ثانیه‌ای.
```

**۲. تقویم ۲۰ قسمت اول**
```
از ۱۰ ایده‌ی انتهای فایل ۰۳ شروع کن و به ۲۰ برسان. برای هرکدام فقط: عنوان،
Twist ضدشهود در یک جمله، و منبع اولیه. بعد مرتبشان کن طوری که ۴ ویدیوی
پشت‌سرهم از یک زیرشاخه (مثلاً حواس) نباشد. قانون «هر ۴ ویدیو یک‌بار ساختار
را عمداً بشکن» را هم داخل تقویم علامت بزن.
```

**۳. تست هوک قبل از ساخت کامل**
```
سیستمی طراحی کن که قبل از تولید ۱۲ پلان، فقط ۳ ثانیه‌ی اول ویدیو در ۳ نسخه
ساخته شود تا بشود ارزان تست کرد. مشخص کن با چه معیاری برنده انتخاب می‌شود.
```
